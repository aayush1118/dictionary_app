{
	"name": "dictionary_app",
	"version": "1.0.0",
	"description": "",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "cd client && npm run build",
		"start": "node app.js",
		"install-client": "cd client && npm install",
		"heroku-postbuil": "npm run install-client && npm run build"
	},
	"author": "aayush jain",
	"license": "ISC",
	"dependencies": {
		"axios": "^0.21.1",
		"cors": "^2.8.5",
		"express": "^4.17.1",
		"mongoose": "^5.11.12"
	}
}
